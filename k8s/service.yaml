apiVersion: v1
kind: Service
metadata:
  name: chartmuseum-manager
  labels:
    app: chartmuseum-manager
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
  selector:
    app: chartmuseum-manager
